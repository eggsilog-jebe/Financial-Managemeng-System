@echo off
powershell -NoProfile -ExecutionPolicy Bypass -Command "if (Test-NetConnection -ComputerName 127.0.0.1 -Port 6379 -InformationLevel Quiet) { Write-Host 'Redis server is already running on port 6379.' -ForegroundColor Green } else { $redisPath = (Get-ChildItem -Path \"$env:LOCALAPPDATA\Microsoft\WinGet\Packages\" -Filter 'redis-server.exe' -Recurse -ErrorAction SilentlyContinue | Select-Object -ExpandProperty FullName -First 1); if ($redisPath) { Start-Process -FilePath $redisPath -WindowStyle Hidden; Write-Host 'Redis server started in background.' -ForegroundColor Green } else { Write-Host 'Redis server executable not found. Try running winget install taizod1024.redis-windows-fork' -ForegroundColor Red } }"
